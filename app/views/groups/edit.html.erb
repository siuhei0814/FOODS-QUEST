<h2>コミュニティ編集</h2>

<%= form_with model:@group, local:true do |f| %>
  <h4>コミュニティ名</h4>
  <%= f.text_field :name %>

   <h4>コンセプト</h4>
  <%= f.text_field :introduction %>

  <h4>料理の写真</h4>
  <%= f.attachment_field :group_image %>

  <%= f.submit "変更を保存" %>
<% end %>